import React from 'react';
import styled from 'styled-components';
import Flex from '@ac/Flex';
import Typography from '@ac/Typography';

export default function Checklist(props) {
  const {
    onChange,
    value,
    options,
  } = props;

  return (
    <List.Column gridGap="6px" fullWidth>

    </List.Column>
  );
}
